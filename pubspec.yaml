name: orientation_picker
description: Display (or hide) Flutter widgets based on orientation.
version: 0.0.1-dev.4
homepage: https://github.com/matt-addams/orientation_picker

environment:
  sdk: ">=2.8.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

